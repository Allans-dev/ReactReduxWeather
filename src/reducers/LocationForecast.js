const locationForecast = (state, action) => {
  if (state === undefined) {
    return {
      // note add something
    };
  } else {
    switch (action.type) {
      case :

        break;
      default:

    }
  }
}

export default locationForecast;
